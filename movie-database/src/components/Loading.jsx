import loading from '/assets/gifs/loading.gif';

const Loading = () => {
    
    return (
        <img src={loading} alt="Loading" className="block my-0 mx-auto" id="loading" />
    )

}

export default Loading
